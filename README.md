# ตารางเวลาและจุดจอดรถไฟฟ้า

ระบบติดตามรถเมล์ไฟฟ้าแบบเรียลไทม์สำหรับมหาวิทยาลัยราชภัฏนครปฐม

## คุณสมบัติ

- 🚌 ติดตามตำแหน่งรถเมล์ไฟฟ้าแบบเรียลไทม์ (3 คัน)
- 🗺️ แผนที่เส้นทางแบบวนรอบ (10 จุดจอด)
- ⏰ ประมาณการเวลาถึงแต่ละจุดจอด
- 📅 ตารางเวลาเดินรถ
- 📍 ข้อมูลจุดจอดและสิ่งอำนวยความสะดวก
- 📝 ระบบรายงานปัญหา
- 📱 รองรับการใช้งานบนมือถือ

## การติดตั้ง

```bash
npm install
```

## การรันโปรเจค

```bash
npm run dev
```

เปิดเบราว์เซอร์ที่ http://localhost:3000

## การ Build

```bash
npm run build
```

## Deploy บน Vercel

### วิธีที่ 1: Deploy ผ่าน Vercel CLI

1. ติดตั้ง Vercel CLI:
```bash
npm install -g vercel
```

2. Login เข้า Vercel:
```bash
vercel login
```

3. Deploy:
```bash
vercel
```

4. Deploy เป็น Production:
```bash
vercel --prod
```

### วิธีที่ 2: Deploy ผ่าน Vercel Dashboard

1. Push โค้ดขึ้น GitHub
2. ไปที่ [vercel.com](https://vercel.com)
3. คลิก "Import Project"
4. เลือก Repository ของคุณ
5. Vercel จะตรวจจับการตั้งค่าอัตโนมัติจาก `vercel.json`
6. คลิก "Deploy"

### การตั้งค่า Vercel

โปรเจคนี้มีไฟล์ `vercel.json` ที่ตั้งค่าไว้แล้ว:
- Build Command: `npm run build`
- Output Directory: `build`
- Framework: Vite
- SPA Routing: รองรับ

## เทคโนโลยีที่ใช้

- React 18
- TypeScript
- Vite
- Tailwind CSS
- React Router
- Lucide Icons
- Radix UI

## โครงสร้างโปรเจค

```
├── src/
│   ├── components/      # React components
│   ├── data/           # ข้อมูลสถานีและตารางเวลา
│   ├── hooks/          # Custom hooks
│   ├── types/          # TypeScript types
│   └── styles/         # CSS files
├── public/             # Static assets
├── vercel.json         # Vercel configuration
└── vite.config.ts      # Vite configuration
```

## License

MIT

---

© 2026 มหาวิทยาลัยราชภัฏนครปฐม
